extends ../layout

block content
  .pb-2.mt-2.mb-4.border-bottom
    h3
      | Your Clients
      a.btn.btn-success.float-right(href='/clients/new') Create client
  
  ul
    for client in clients
      li: a(href=`/clients/${client.id}`)= client.name